import { describe, expect, it } from "vitest"

/**
 * Placeholder — Strava sync unit coverage lives in @pacepilot/strava.
 * Inngest Strava workers deferred; sync runs in apps/web.
 */
describe("api jobs scaffold", () => {
  it("keeps a trivial assertion so vitest has a suite", () => {
    expect(true).toBe(true)
  })
})
